define LiteralValues {
  # String literals
  role: &literal(admin)
  environment: &literal(production)
  
  # Number literals
  version: &literal(1)
  apiVersion: &literal(2)
  
  # Boolean literals
  isEnabled: &literal(true)
  isLegacy: &literal(false)
}
